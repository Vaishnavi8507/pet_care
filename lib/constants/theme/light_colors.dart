// light_colors.dart
import 'package:flutter/material.dart';

class LightColors {
  // Primary colors
  static const Color primaryColor = Color(0xFF9350F3);
  static const Color primaryDarkColor = Color(0xFFC06824);

  // Accent colors
  static const Color accentColor = Color(0xFFFFC107);
  static const Color accentDarkColor = Color(0xFFFF6F00);

  // Text colors
  static const Color textColor = Color(0xFF333333);
  static const Color textColorLight = Color(0xFFFFFFFF);
  static const Color errorColor = Color(0xFFD32F2F);

  // Background colors
  static const Color backgroundColor = Color(0xFFF5F5F5);
  static const Color scaffoldBackgroundColor = Color(0xFFFAFAFA);

  // Button colors
  static const Color buttonColor = Color(0xFF3F93E7);
  static const Color buttonTextColor = Color(0xFFFFFFFF);
  static const Color buttonDisabledColor = Color(0xFFBDBDBD);

  // Border colors
  static const Color borderColor = Color(0xFFCCCCCC);

  // Placeholder colors
  static const Color placeholderColor = Color(0xFFBDBDBD);

  // Shadow color
  static const Color shadowColor = Color(0x40000000);

  // Example custom color
  static const Color customColor = Color(0xFF8BC34A);
}
